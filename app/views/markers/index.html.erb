<p id="notice"><%= notice %></p>
<%= render 'shared/navbar' %>
<%= render 'markers/form', marker: @marker %>
<h1>Markers</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Category</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @markers.each do |marker| %>
      <%= render 'markers/marker', marker: marker  %>
    <% end %>
  </tbody>
</table>
<br>
